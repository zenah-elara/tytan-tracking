import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function EmployeeAttendancePage() {
  return (
    <PlaceholderPage
      module="Employee"
      title="Attendance"
      description="Future attendance history view with daily status, late marks, absences, overtime, and manual adjustment status."
    />
  );
}
