import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function EmployeeSchedulePage() {
  return (
    <PlaceholderPage
      module="Employee"
      title="Schedule"
      description="Future schedule view showing shift start, shift end, grace period, day-offs, and timezone."
    />
  );
}
