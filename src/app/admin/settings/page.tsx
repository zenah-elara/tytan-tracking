import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function AdminSettingsPage() {
  return (
    <PlaceholderPage
      module="Admin"
      title="Settings"
      description="Future configuration area for system preferences. Supabase and Google Workspace settings are intentionally not configured yet."
    />
  );
}
